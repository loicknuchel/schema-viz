module Libs.Models exposing (FileContent, FileName, HtmlId, Text)

-- deps = { to = { only = [ "Libs.*" ] } }


type alias HtmlId =
    String


type alias Text =
    String


type alias FileName =
    String


type alias FileContent =
    String
